#While play the quiz game

# It will show the question

for question in self.questions:
  print(question["question:])
  self.display_options(question["options"])

# if you got currect answer

if user_answer == question["corect_answer"]:
  print(Correct\n")
self.score += 1

# if you got incurrect answer

print("Wrong!")
